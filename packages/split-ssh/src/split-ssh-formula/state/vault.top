user:
  qubes:tags:split-ssh-vault-template:
    - match: pillar
    - split-ssh.vault.packages

  qubes:tags:split-ssh-vault:
    - match: pillar
    - split-ssh.vault.config
    - split-ssh.vault.rpc
